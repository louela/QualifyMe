@model QualifyMe.ViewModels.DepartmentView
@{
    ViewBag.Title = "View Department";
}

<div class="row">
    <div class="col-sm-12">
    
        <div class="form-group form-row">
            <div class="col-sm-10 offset-sm-2">
                <h3>@Model.DepartmentName</h3>
                <div class="row">

                   

                </div>




                @for(int i = 0; i < Model.Courses.Count; i++)
                {
                   
                       

                        <div class="col-sm-7">
                            @if (Convert.ToInt32(Session["CurrentDepartmentID"]) != Model.Courses[i].DepartmentID)
                            {
                                @Model.Courses[i].CourseName
                            }
                             
                            
                        </div>

                    
                      
                    
                }



                @if (Session["CurrentUsername"] != null)
                {
                    <form class="needs-validation" novalidate="novalidate" action="/admin/departments/addcourse" method="post" id="newForm">
                        <div class="form-group form-row">
                            <div class="col-sm-12">
                                <h4>Add Course</h4>

                                @Html.AntiForgeryToken()
                                <input type="hidden" name="DepartmentID" value="@Model.DepartmentID" />
                                <div class="form-row">
                                    <div class="form-group col-sm-12">
                                        <textarea class="form-control" id="CourseName" placeholder="Enter Course Name" name="CourseName" required="required" data-pattern="[A-z0-9 ,-]{1,}"></textarea>
                                        <div class="valid-feedback">
                                            Looks good!
                                        </div>
                                        <div class="invalid-feedback">
                                            Please enter answer.
                                        </div>
                                    </div>
                                </div>

                                <button type="submit" class="btn btn-primary" data-formid="newForm">Add Course</button>
                                <div class="text-danger">
                                    @Html.ValidationSummary()
                                </div>
                            </div>
                        </div>
                    </form>
                }
            </div>

              
           
        </div>
    
        
    </div>
</div>









<script>
    $(".editbutton").click(function () {
        $(this).parent().find(".needs-validation").show();
        $(this).hide();
        $(this).parent().find(".answer").hide();
    });
</script>

