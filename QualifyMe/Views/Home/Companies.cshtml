@model List<QualifyMe.ViewModels.CompanyView>

@{
    ViewBag.Title = "Companies";
    Layout = "~/Views/Shared/_LayoutPage1.cshtml";
}
<link rel="stylesheet" href="~/content/companylist.css">
<div class="shell">
    <div class="container">
        <div class="row">
            @foreach (var item in Model)
            {
                <div class="col-md-3">
                    <div class="wsk-cp-product">
                        <div class="wsk-cp-img">
                            <img src="~/Images/Company/accenture.png" alt="Company" class="img-responsive" />
                        </div>
                        <div class="wsk-cp-text">
                            <div class="category">
                                <span><a href="#">View Profile</a></span>
                            </div>
                            <div class="title-product">
                                <h3>@item.CompanyName</h3>
                            </div>
                            <div class="description-prod">
                                <p>@item.CompanyDescription</p>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</div>

